//All endpoints related to modifying database regarding session etc
import { randomUUID } from 'crypto';
import { Router } from 'express';

const router = Router();

router.post('/create', async (req,res) => {

})

export default router;


